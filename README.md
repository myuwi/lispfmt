<div align="center">

# lispfmt

</div>

A universal formatter for the Lisp family of programming languages.

## Usage

To use `lispfmt`, simply pipe the input into the `lispfmt` command via stdin, and the formatted output will be printed to stdout.

```sh
cat input.fnl | lispfmt
```
